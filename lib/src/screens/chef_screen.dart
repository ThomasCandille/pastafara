import 'package:flutter/material.dart';

class ChefView extends StatelessWidget {
  const ChefView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text("Chef View"));
  }
}
